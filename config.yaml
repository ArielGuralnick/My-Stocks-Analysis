# ============================================================
# Portfolio Monitor — User Configuration
# ============================================================
# Edit this file to customize the monitor for your portfolio.
# You do NOT need to touch any Python code.
#
# After editing, restart portfolio_monitor.py / dashboard.py.
# ============================================================

# ------------------------------------------------------------
# Excel settings
# ------------------------------------------------------------
excel:
  # Full path to your Excel file exported from Excellence brokerage.
  # Leave empty ("") to auto-detect the latest Excellence_*.xlsx in ~/Downloads.
  # You can also upload the file directly from the Dashboard (Upload Excel button).
  file: "C:/Users/ArielGuralnick/Downloads/נתוני חשבון.xlsx"

  # Name of the column in the Excel that contains security names.
  ticker_column: "שם נייר"

  # Row index (0-based) where the header row is located in the Excel sheet.
  header_row: 9

# ------------------------------------------------------------
# Ticker map
# ------------------------------------------------------------
# Map each security name (exactly as it appears in your Excel) to its
# Yahoo Finance ticker symbol.
#
# How to add a new stock:
#   1. Open your Excel export and copy the exact name from the "שם נייר" column.
#   2. Find the Yahoo Finance ticker (e.g. search on finance.yahoo.com).
#   3. Add a line below:  "EXACT NAME FROM EXCEL": "TICKER"
# ------------------------------------------------------------
ticker_map:
  "APPLE COMPUTER": "AAPL"
  "BITB": "BITB"
  "META PLATFORMS INC": "META"
  "MICROSOFT CORP": "MSFT"
  "MICROSTRATEGY INC": "MSTR"
  "NVIDIA CORP": "NVDA"
  "SOFI TECHNOLOGIES INC.": "SOFI"
  "TESLA MOTORS INC": "TSLA"
  "iShares CORE MSCI EUROPE UCITS ETF EUR-TA": "IMEU.L"
  "iShares CORE MSCI EM IMI UCITS ETF-TA": "EIMI.L"
  "iShares $ CORE S&P 500 UCITS ETF-TA": "CSPX.L"
  "iShares $ NASDAQ 100 UCITS ETF-TA": "CNDX.L"
  "אל על": "ELAL.TA"
  # TODO: find Yahoo Finance tickers for these two:
  # "אי.בי.אי. מניות תעשיות ביטחוניות ישראל": "???"
  # "קסם 4A) ETF) תא 35": "???"
